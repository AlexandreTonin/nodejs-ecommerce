import { Link } from 'react-router-dom';
import Form from '../components/Form';

export default function LoginPage() {
    return (
        <div className='flex flex-col mt-20 2xl:mt-40 h-screen gap-12 font-sans'>
            <Form></Form>
        </div>
    )
}